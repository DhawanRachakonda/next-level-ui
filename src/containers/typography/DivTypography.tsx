import React from 'react';

import { withStyles } from '@material-ui/styles';

import Typography from '@material-ui/core/Typography';

const DivTypography = withStyles((theme) => ({}))((props: any) => (
  <Typography {...props} />
));

export default DivTypography;
